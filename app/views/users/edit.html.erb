<!DOCTYPE html>
<html>
<head>
  <% provide(:title, t("user.edit"))%>
  <% provide(:base_title, t("base.title")) %>
  <% provide(:button_text, t("user.edit_submit")) %>
</head>
<body>
  <h1><%= t("user.edit_page") %></h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render "form" %>
      <div class="gravatar_edit">
        <%= gravatar_for @user %>
        <%= link_to t("user.edit"), "http://gravatar.com/emails", target: "_blank" %>
      </div>
    </div>
  </div>
</body>
</html>

