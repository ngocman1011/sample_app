<!DOCTYPE html>
<html>
<head>
  <% provide(:title, t("user.signup"))%>
  <% provide(:base_title, t("base.title")) %>
  <% provide(:button_text, t("user.submit")) %>
</head>
<body>
  <h1><%= t("user.signup") %></h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render "form" %>
    </div>
  </div>
</body>
</html>

